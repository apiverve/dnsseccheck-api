name: apiverve_dnsseccheck
description: DNSSEC Checker validates the DNSSEC (Domain Name System Security Extensions) configuration of a domain. It verifies that DNS responses are authenticated and haven't been tampered with.
version: 1.1.14
homepage: https://apiverve.com/marketplace/dnsseccheck?utm_source=dart&utm_medium=homepage
repository: https://github.com/apiverve/dnsseccheck-api/tree/main/dart

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.1.0
  http_parser: ^4.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
